version: 2
updates:
  # The project has zero npm dependencies, so only the GitHub Actions used by
  # the CI and release workflows need update monitoring.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
